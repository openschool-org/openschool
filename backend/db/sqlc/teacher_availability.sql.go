// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: teacher_availability.sql

package db

import (
	"context"

	"github.com/google/uuid"
)

const createTeacherAvailability = `-- name: CreateTeacherAvailability :one
INSERT INTO teacher_availability (teacher_id, academic_year_id, day_of_week, period_number)
VALUES ($1, $2, $3, $4)
ON CONFLICT (teacher_id, academic_year_id, day_of_week, period_number) DO NOTHING
RETURNING id, teacher_id, academic_year_id, day_of_week, period_number, created_at
`

type CreateTeacherAvailabilityParams struct {
	TeacherID      uuid.UUID `json:"teacher_id"`
	AcademicYearID uuid.UUID `json:"academic_year_id"`
	DayOfWeek      int16     `json:"day_of_week"`
	PeriodNumber   int16     `json:"period_number"`
}

func (q *Queries) CreateTeacherAvailability(ctx context.Context, arg CreateTeacherAvailabilityParams) (TeacherAvailability, error) {
	row := q.db.QueryRow(ctx, createTeacherAvailability,
		arg.TeacherID,
		arg.AcademicYearID,
		arg.DayOfWeek,
		arg.PeriodNumber,
	)
	var i TeacherAvailability
	err := row.Scan(
		&i.ID,
		&i.TeacherID,
		&i.AcademicYearID,
		&i.DayOfWeek,
		&i.PeriodNumber,
		&i.CreatedAt,
	)
	return i, err
}

const deleteTeacherAvailability = `-- name: DeleteTeacherAvailability :exec
DELETE FROM teacher_availability
WHERE id = $1
`

func (q *Queries) DeleteTeacherAvailability(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteTeacherAvailability, id)
	return err
}

const isTeacherUnavailable = `-- name: IsTeacherUnavailable :one
SELECT EXISTS (
    SELECT 1 FROM teacher_availability
    WHERE teacher_id = $1 AND academic_year_id = $2 AND day_of_week = $3 AND period_number = $4
) AS unavailable
`

type IsTeacherUnavailableParams struct {
	TeacherID      uuid.UUID `json:"teacher_id"`
	AcademicYearID uuid.UUID `json:"academic_year_id"`
	DayOfWeek      int16     `json:"day_of_week"`
	PeriodNumber   int16     `json:"period_number"`
}

func (q *Queries) IsTeacherUnavailable(ctx context.Context, arg IsTeacherUnavailableParams) (bool, error) {
	row := q.db.QueryRow(ctx, isTeacherUnavailable,
		arg.TeacherID,
		arg.AcademicYearID,
		arg.DayOfWeek,
		arg.PeriodNumber,
	)
	var unavailable bool
	err := row.Scan(&unavailable)
	return unavailable, err
}

const listTeacherAvailabilityByTeacherYear = `-- name: ListTeacherAvailabilityByTeacherYear :many
SELECT id, teacher_id, academic_year_id, day_of_week, period_number, created_at FROM teacher_availability
WHERE teacher_id = $1 AND academic_year_id = $2
ORDER BY day_of_week ASC, period_number ASC
`

type ListTeacherAvailabilityByTeacherYearParams struct {
	TeacherID      uuid.UUID `json:"teacher_id"`
	AcademicYearID uuid.UUID `json:"academic_year_id"`
}

func (q *Queries) ListTeacherAvailabilityByTeacherYear(ctx context.Context, arg ListTeacherAvailabilityByTeacherYearParams) ([]TeacherAvailability, error) {
	rows, err := q.db.Query(ctx, listTeacherAvailabilityByTeacherYear, arg.TeacherID, arg.AcademicYearID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []TeacherAvailability{}
	for rows.Next() {
		var i TeacherAvailability
		if err := rows.Scan(
			&i.ID,
			&i.TeacherID,
			&i.AcademicYearID,
			&i.DayOfWeek,
			&i.PeriodNumber,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
